<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio para imágenes capturadas -->
    <external-files-path
        name="images"
        path="Pictures/" />

    <!-- Directorio raíz de archivos externos -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Directorio de cache para archivos temporales -->
    <cache-path
        name="cache"
        path="." />

    <!-- Directorio interno de archivos -->
    <files-path
        name="files"
        path="." />

    <!-- Directorio externo del almacenamiento -->
    <external-path
        name="external"
        path="." />
</paths>